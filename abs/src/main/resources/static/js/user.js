// script.js

// Example: Simple log to confirm the file is linked
document.addEventListener('DOMContentLoaded', function() {
    console.log('User module scripts loaded.');

    // You could add custom form validation here, for example:
    // const form = document.querySelector('form');
    // form.addEventListener('submit', function(event) {
    //     // if (!validateContactNo(document.getElementById('contactNo').value)) {
    //     //     alert('Please enter a valid contact number.');
    //     //     event.preventDefault();
    //     // }
    // });
});